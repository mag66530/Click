# Версии Click и откат

Файл нужен, чтобы «откати» и «доработай» были однозначными командами, а не
поводом гадать, какую именно версию возвращать.

Текущая версия видна в самом приложении: нажать плашку проекта в шапке –
там строка «Версия».

## Как устроено

| Ветка | Что это |
|---|---|
| `main` | То, что работает у заказчика. Сюда заливается каждая готовая правка. |
| `rollback-good` | Последняя версия, про которую заказчик **сам сказал**, что работает. Метка не двигается, пока это не подтверждено. |
| `before-reviews` | Снимок `main` за минуту до заливки отзывов. Свежий запасной аэродром: новее, чем `rollback-good`, но без новой фичи. |
| `before-kp-audit` | Снимок `main` за минуту до заливки сверки с Яндексом. |
| `before-2gis` | Снимок `main` за минуту до заливки 2ГИС. |
| `before-crosspost-calendar` | Снимок `main` за минуту до заливки нового вида вкладки «Кросспостинг». |
| `before-feed-verify` | Снимок `main` за минуту до заливки проверки публикации по ленте. Новый вид вкладки «Кросспостинг» (тогда – календарём) в него уже входит. |
| `before-gis-patience` | Снимок `main` за минуту до заливки правок 2ГИС (ожидание страницы, кнопка обновления городов) и разбора отказов Gemini. Отчёт «только сделанное», сверка текста МАКС и жирный в ОК в него уже входят. **Самый свежий запасной аэродром.** |
| `click-data` | **НЕ трогать.** Данные приложения: сессии Яндекса и 2ГИС, реестр публикаций, окончания постов. Ветка живёт отдельно от кода и в `main` не вливается никогда – облако её не отслеживает (см. `repo_store.py`). Удалить её значит стереть заказчику сохранённые входы. |
| `claude/mpi-repo-report-ufdwk0` | Отчёты по дням за 3–7 августа. В `main` намеренно не вливается – это записи о работе, а не код. Оставлена как есть. |

Смысл разделения: `main` может уехать вперёд и сломаться, а `rollback-good`
всегда указывает на заведомо живую сборку. Откат – это вернуть `main` на неё.

### Рабочие ветки

Ветка под задачу живёт, пока задача не влита. **Влили в `main` – ветку
удаляем**, иначе через месяц их два десятка и не видно, где идёт работа:

```
git push origin --delete <имя-ветки>
```

Что осталось от прежнего порядка «ветка влита, но живёт дальше»: ничего.
Дорабатывать в старой влитой ветке смысла нет – она отстаёт от `main` тем
сильнее, чем дольше лежит. Новая задача – новая ветка от свежего `main`.

Не удаляются только четыре вида: `main`, `click-data`, `rollback-good` и
снимки `before-*`. Всё остальное после слияния – мусор.

## Откат

**Убрать правки 2ГИС и разбор отказов Gemini, всё остальное оставить:**

```
git push origin origin/before-gis-patience:refs/heads/main --force-with-lease
```

Нужно, если 2ГИС стал работать заметно медленнее и это мешает: города без
плашки и без отзывов теперь стоят по девять секунд вместо двух – это цена
того, чтобы «плашки нет» и «отзывов нет» были правдой. Разбор отказов Gemini
откатится вместе с ними, и текст ошибки снова станет общим.

**Убрать проверку публикации по ленте, всё остальное оставить:**

```
git push origin origin/before-feed-verify:refs/heads/main --force-with-lease
```

Нужно, если статус в отчёте разошёлся с тем, что видно в карточке глазами:
проверка читает ленту Яндекса, а его разметку мы не контролируем. После
отката статус снова ставится по ответу API – со всеми прежними ложными
«опубликовано», поэтому откатывать стоит только если новая проверка врёт
чаще старой.

**Убрать 2ГИС и самообновление городов, всё остальное оставить:**

```
git push origin origin/before-2gis:refs/heads/main --force-with-lease
```

Код 2ГИС при этом никуда не девается – он в истории `main`. Правим его в
новой ветке от свежего `main` и вливаем заново, когда заработает.

**Убрать новый вид вкладки «Кросспостинг», всё остальное оставить:**

```
git push origin origin/before-crosspost-calendar:refs/heads/main --force-with-lease
```

Кросспостинг при этом остаётся: вернётся прежний вид – четыре цифры и
раскрывашки подряд. Новый вид (строки плана с колонками площадок) живёт в
`crosspost_plan.py` и в `tab_crosspost`, механика отложек его не касается.
Осторожно: откат на эту метку снимет и всё, что залито после неё, – проверка
публикации по ленте в том числе.

**Убрать только сверку с Яндексом:**

```
git push origin origin/before-kp-audit:refs/heads/main --force-with-lease
```

Код сверки остаётся в истории `main`: правим его в новой ветке и вливаем
заново, когда сверка себя покажет.

**Быстрый – убрать и отзывы тоже:**

```
git push origin origin/before-reviews:refs/heads/main --force-with-lease
```

**Полный – вернуться к последней подтверждённой заказчиком сборке:**

```
git push origin origin/rollback-good:refs/heads/main --force-with-lease
```

После отката доработка продолжается в рабочей ветке от этой же точки –
ничего не теряется, сломанные коммиты остаются в истории ветки.

Отдельно про отзывы: фича опциональная. Если она мешает, а откатывать код
не хочется – достаточно **не ставить галочку** «Заодно проверить отзывы»
в «Актуализации». С выключенной галочкой прогон идёт ровно как раньше и в
раздел отзывов не заходит вовсе.

## Когда двигать `rollback-good`

Только после того, как заказчик проверил сборку на живой публикации и
сказал, что всё хорошо:

```
git push origin main:refs/heads/rollback-good --force-with-lease
```

## Журнал

| Версия | Коммит | Что внутри | Проверено заказчиком |
|---|---|---|---|
| 2026-08-14-max-one-insert | – | **МАКС снова не поставил отложку: «МАКС отправил строку вместо переноса – после Shift+Enter поле опустело».** В канале при этом ничего не появилось, а на снимке заказчицы весь пост стоял в поле ввода – и под ним хвост прошлой попытки. То есть проверка снова оговорила ввод, как и накануне с обрезанным отпечатком. Разбор дал три отдельные беды. **(1) Читали не то поле.** Под селектор поля у МАКС подходит несколько узлов: рядом с живым редактором лежит его пустышка-подсказка. Брали `.first` – и клик мог уйти в пустышку, а чтение возвращало пустоту. Теперь поле выбирается по размеру: настоящее – самое крупное из подходящих. **(2) «В поле пусто» объявляли отправкой.** Это была догадка: в канал никто не смотрел. Теперь перед вводом снимается состояние ленты, и «мы отправили» означает ровно одно – в канале появилось сообщение, которого там не было. Вчерашний пост за свежую отправку не выдаётся: он был в ленте и до нас (об этом прямо просила заказчица). **(3) Переносы строк больше не нажимаются вовсе.** Весь текст, вместе с переносами, вставляется ОДНИМ вызовом insert_text – это событие ввода, а не клавиша: keydown не возникает, и отправить оно не может физически. Shift+Enter остался только запасным путём, если сборка МАКС перестанет принимать переносы одним куском, и проверяется он по каналу – заметив отправку, Click останавливается на первой строке и говорит, что удалить. **И четвёртое, из-за чего пост склеивался:** поле не очищалось перед вводом, и текст дописывался к остатку неудачной попытки («…задачВ наличии:»). Теперь поле очищается с проверкой, а не удалось – Click не вводит ничего. Проверки идут на настоящем поле в браузере: обычный ввод, остаток в поле, сборка без переносов и худший случай «Shift+Enter отправляет». | нет |
| 2026-08-14-gemini-key-verdict (в main под меткой 2026-08-14-gis-and-gemini) | `17084f6` | **«Не получается переписать отзыв, хотя ключи заданы».** Click отвечал на это одной строкой: «Google не принял ключ Gemini. Проверьте секрет gemini_api_key». Строка была не разбором, а догадкой – её писали на ЛЮБОЙ ответ 400 и 403, а ответ 401 не разбирали вовсе. Причин же много и они разные: ключ недействителен, для проекта не включён Generative Language API, у ключа ограничения по адресу, кончилась квота – или в поле лежит вовсе не ключ. Последнее и было в жизни: значение вида `AQ.Ab8…` (55 знаков) – это учётные данные OAuth, а не ключ API. Проверено запросами к самому Google: он смотрит на начало значения, `AIza…` принимает как ключ (плохой даёт 400 «API key not valid»), а `AQ.…` отвечает 401 «Expected OAuth 2 access token» – одинаково в заголовке, в `?key=` и в Bearer. Теперь Click: показывает ответ САМОГО Google своими словами и с его цитатой; предупреждает про формат прямо в поле ключа, до всякого запроса; выводит непринятый ключ из круга на весь вызов (раньше плохой ключ ходил по кругу до конца бюджета, и человек видел его ошибку, хотя рядом лежал рабочий); называет ключи по именам секретов – видно, какой из трёх чинить; а прогон, не получив ни одного принятого ключа, сразу перестаёт писать черновики вместо того, чтобы повторить ту же ошибку на всех городах. Добавлена кнопка **«🔎 Проверить ключи»** – по одному дешёвому запросу на ключ, с приговором по каждому. **И мелочь, которая сбивала с толку:** под ПУСТЫМ полем ответа висело «черновик оборван или в нём остались служебные заметки модели» – теперь там честное «Черновика нет – причина выше». | нет |
| 2026-08-14-gis-patience (в main под меткой 2026-08-14-gis-and-gemini) | `c439951` | **Четыре беды 2ГИС из одного прогона, и три из них – один и тот же корень: Click судил о странице раньше, чем кабинет успевал её нарисовать.** (1) **Отзывов «нет», а они есть.** В Оше отзыв висел, а прогон закрывал страницу через две секунды и писал «отзывов пока нет». Готовность раздела определялась по слову «отзыв» в тексте страницы – а «Отзывы» написано в МЕНЮ кабинета, то есть проверка была истинной всегда, ещё до первой отрисовки. Теперь готовность – это следы самого раздела (фильтр «Без ответа», звёзды карточек, прямая надпись кабинета «нет отзывов»), плюс выдержанный срок: раньше девяти секунд «отзывов нет» не говорим. Не дорисовалась совсем – страница перезагружается ещё раз, и только потом честный отказ со словами «отзывы могли остаться незамеченными», а не тихое «ноль». (2) **«Плашки „Данные верны“ нет» на всех городах подряд.** Та же семья: сама страница успокаивается за пару секунд, а плашку кабинет забирает отдельным запросом и рисует позже. Тишина больше не приговор – сначала выдерживаем срок (9 секунд), и только потом «плашки нет» становится ответом; заодно ждём тишины в сети после открытия. (3) **«Файлы переданы, но в альбоме они не появились», хотя фото в кабинете лежит.** Проверка после загрузки была одна и сразу: Click перезагружал раздел и считал плитки на ещё пустой странице. Теперь альбом перепроверяется заходами с растущей паузой (2, 4, 8 секунд), плитки считаются только на дорисованном альбоме, а перед счётом страница прокручивается до низа – иначе новый снимок «теряется среди существующих» и не попадает в поле зрения вовсе. (4) **Список городов 2ГИС нечем было обновить.** Кнопка «Загрузить города из КП» жила только в «Настройках», а человек ищет её там, где выбирает города: поставили Шымкенту статус «Активная» – города в списке нет, и позвать его туда нечем. Кнопка «⟳ Обновить города из КП» теперь стоит и на «Актуализации» (со счётом городов ИМЕННО этой площадки и датой последней загрузки), и на «Городах». А после загрузки Click говорит, какие города НЕ попали в список 2ГИС и почему – по имени: «Шымкент – статус «Удалена»», «Актобе – в колонке 2ГИС не ссылка на кабинет». | нет |
| 2026-08-07-gis-media-confirmed | – | **Отчёт писал «снимков добавлено 1 из 1», а в самом кабинете 2ГИС альбом оставался пустым.** Причина: успех загрузки проверялся по тому, что нарисовал в интерфейсе БРАУЗЕР – а кабинет добавляет плитку в альбом «оптимистично», сразу после выбора файла, ещё до того, как сервер его принял. Если сервер файл отклонял, плитка тихо пропадала, но Click этого уже не видел: цикл ожидания выходил, как только замечал рост, и на этом останавливался. Теперь после ожидания страница ПЕРЕЗАГРУЖАЕТСЯ, и в счёт идёт только то, что покажет свежий ответ сервера, – ровно то, что заказчик увидит сама, зайдя в раздел «Фото и видео». Проверка на этот случай: клиент рисует плитку, сервер её не подтверждает – без перезагрузки тест воспроизводит ровно баг (получили 1, ждали 0), с ней проходит. | нет |
| 2026-08-07-gis-own-queue | – | **Прошлая правка от разнобоя сборок не сработала – и вот почему.** Проверка сверяла модули с меткой ИЗ ПАМЯТИ, а Streamlit перечитывает с диска только главный экран: после обновления старыми оказывались и модули, И МЕТКА – всё «сходилось» при разном коде, и проверка молчала. Заказчик получила ещё два пустых поста (Баку, Ташкент) и снова «Invalid url» в 2ГИС. Теперь метка читается С ДИСКА, а отставшие модули Click перечитывает сам (importlib.reload, только когда метки разошлись, только когда не идёт ни один прогон и под общим замком) – и продолжает работу. Не удалось – страница ничего не рисует и ничего не запускает. **Задания «только фото в 2ГИС» переехали в СВОЮ очередь** (`tasks-gis-photos`). Это главная защита: прогон прежней сборки в эту папку не заглядывает вовсе, потому что в его коде её нет – очередь для него пуста, и пустых постов быть не может в принципе. Если в очереди окажутся сразу посты и фото в 2ГИС, запуск блокируется, пока одно из двух не убрано; у каждой очереди своя кнопка очистки. | нет |
| 2026-08-07-one-build | – | **Приложение больше не работает вразнобой – и в этом была причина сразу двух бед.** Облако обновляет файлы под работающим приложением, и Streamlit выселяет изменённые модули по одному: в промежутке главный экран УЖЕ новый, а сосед ЕЩЁ старый. На «Актуализации» это дало «AttributeError: casual_words» (экран новый, reviews старый). На «Публикации» – хуже: новый экран сложил задачи «только фото в 2ГИС», а прогон старым кодом про такие задачи не знал и опубликовал в Москве, Петербурге и Новосибирске ПУСТЫЕ посты. Теперь метка сборки сверяется у всех частей сразу: не совпало – страница пишет «Приложение обновляется – секунду», ничего не рисует и ничего не запускает, а через мгновение возвращается уже на одной сборке. Перезагружать модули самим по-прежнему нельзя – это роняло приложение целиком. **Пустой пост не публикуется никогда:** проверка стоит и у прогона, и у самой кнопки «Создать» – пустой пост в карточке заказчика не исправить ничем, кроме рук. **У отчёта появился заголовок прогона** – «Публикация · отгрузка + фото в «Товары» + фото в 2ГИС», «Публикация · информационный пост», «Только фото в 2ГИС, без постов», «Актуализация 2ГИС + проверка отзывов». Он же первой строкой в логе. **Кнопка «Очистить очередь» вышла на видное место:** она лежала внутри свёрнутого списка файлов, и после остановки прогона очередь было нечем убрать. Рядом сказано, почему недоделанные города остаются. | нет |
| 2026-08-07-gis-only | – | **ВРЕМЕННО: только фото в 2ГИС, без поста.** Под блоком «Товары» появилось отдельное поле «ВРЕМЕННО · Только фото в 2ГИС – без поста»: положили туда снимки, выбрали города – и кнопка сама стала «📸 Фото в 2ГИС». Заведено, чтобы проверить заливку в 2ГИС, не публикуя ради этого шестьдесят постов; потом сольётся обратно с фото Яндекса. По такому заданию прогон не публикует пост, не льёт фото в «Товары» Яндекса и не проверяет аккаунт Яндекса – нужна сессия 2ГИС. Второй проход (проверка ленты постов) такие города тоже не трогает: поста там не было. На «Запуске» это видно – шаг «Фото в 2ГИС», кнопка «Залить фото в 2ГИС», вход спрашивается в 2ГИС. Текст поста и это поле одновременно не работают: разные режимы, Click скажет и не даст добавить в очередь. Повтор тех же снимков в тот же город читается как «пропущено», а не «ошибка». | нет |
| 2026-08-06-gis-photo-fix | – | **Разбор первого прогона с фото в 2ГИС: в 2ГИС не уехало ни одного снимка, три беды подряд.** Первая: ссылку на раздел «Фото и видео» кабинет отдаёт ОТНОСИТЕЛЬНОЙ (`/orgs/…/media`), а Playwright такой адрес не принимает вовсе – «Invalid url». Сохранённая страница, снятая для теста, пришла с развёрнутыми адресами, поэтому проверка беду и не показывала; теперь фикстура как в жизни, а ссылка разворачивается в полный адрес. Вторая: на пятом городе сторож памяти перезапустил браузер, вкладка 2ГИС умерла вместе с ним, а прогон помнил её сам – и все оставшиеся 55 городов получили «Кабинет 2ГИС не открылся: Event loop is closed». Теперь вкладка спрашивается у браузера каждый раз. Третья: меню кабинета дорисовывается позже страницы, и у Екатеринбурга Click уходил раньше, чем оно появлялось – «в кабинете не нашлась ссылка на раздел». Теперь ждём именно ссылку, а не «страница перестала расти». **И падение вкладки «Публикация»:** щелчок по галочке «Опубликовать эти фото ещё и в 2ГИС» ронял весь экран с TypeError – save_config звали с лишним аргументом. Добавлена общая проверка: свои функции должны вызываться с тем числом аргументов, которое они принимают – такие ошибки Питон до выполнения не ловит. | нет |
| 2026-08-06-gis-photos | – | **Фото отгрузки уходят и в 2ГИС.** У поста типа «Отгрузка» под полем «Фото в раздел „Товары“» появилась галочка «Опубликовать эти фото ещё и в 2ГИС». Берутся ТЕ ЖЕ файлы, качаются один раз на обе площадки. Галочка помнит прошлый выбор проекта: первый раз выключена, дальше стоит так, как оставили. **Кнопку 2ГИС жать не надо** – и это главное, что удалось выяснить по сохранённым страницам кабинета. На экране добавление выглядит по-разному: пустой альбом рисует синюю кнопку «Добавить» посередине, непустой – круглый «плюс» в правом нижнем углу. А в разметке и там, и там лежит один и тот же настоящий `input type=file`, файлы отдаются прямо ему. Поэтому неважно, как сегодня выглядит кнопка и куда её передвинули. Адрес раздела (`/orgs/<id>/branches/<филиал>/media`) берётся из меню кабинета, а не складывается из кусков – на складывании адреса Click уже обжёгся с «Данными о компании». **Заливка ничего не ломает:** шаг отдельный, и что бы там ни случилось, статус публикации поста не меняется. Успех считается только доказанный – снимок должен появиться в альбоме, «файлы переданы» успехом не считается. Город без карточки 2ГИС получает спокойное «Нет карточки в 2ГИС». gif отсеивается заранее: 2ГИС его не принимает и молча. **Одни и те же снимки в один город второй раз не заливаются** – реестр считает их по содержимому файлов, а не по именам (одна и та же фотография, скачанная дважды, ложится во временную папку под разными именами). Вторая сессия работает ОТДЕЛЬНЫМ контекстом в уже запущенном браузере, а не вторым браузером: второй стоил бы ещё полгигабайта, и прогон падал бы по памяти. | нет |
| 2026-08-06-mpi-tone | – | **Ответ МПИ пишется деловым языком, а не словами клиента.** «Приятно слышать, что вы отметили классный выбор металла, классные ценники и быструю доставку» – это пересказ отзыва теми же разговорными словами, а отвечает компания. Теперь то же самое звучит так: «Приятно слышать, что вы оценили широкий выбор металла, адекватные цены и оперативную доставку» – суть отзыва сохраняется, слова деловые. В промпте появилось правило про тон с разбором ходовых слов («классный выбор» → «широкий выбор», «ценники» → «цены», «быстро привезли» → «оперативная доставка») и четвёртый образец заказчика ровно на этот случай. Ходовые сочетания код заменяет сам, а что заменить нельзя, не зная смысла («классный менеджер»), показывается предупреждением под черновиком – как и запрещённые слова. | нет |
| 2026-08-06-mpi-thanks | – | **МПИ больше не благодарит за рекомендацию, которой не было.** На отзыв «классный выбор металла, ценники адекватные, доставка быстрая» ответ всё равно начинался со «Спасибо за отзыв и рекомендацию!» – хотя человек нас никому не рекомендовал. Теперь первая строка одна из двух: «Спасибо за отзыв и рекомендацию!», если клиент нас рекомендует или советует, и просто «Спасибо за отзыв!» во всех остальных случаях. Правило прописано в промпте и продублировано кодом, как и остальной вид ответа МПИ; два образца заказчика из трёх переведены на короткую первую строку – в тех отзывах рекомендации нет. Заодно починена кнопка «Проверить генерацию на примере»: после ответа Gemini она падала на подписи к нему. | нет |
| 2026-08-06-local-keys | – | **Веб-сервисы заводятся с локалки без правки файлов.** Черновики отзывов (Gemini), таблица КП (Google) и хранение данных (GitHub) требуют ключей; в облаке они в секретах Streamlit, а локально секретов нет – надо было руками создавать `.streamlit/secrets.toml` и знать его формат. Появился раздел «⚙️ Настройки» → «Ключи к веб-сервисам»: вписал полями, нажал «Сохранить» – работает. Порядок источников не изменился (переменная окружения → секреты Streamlit → настройки), поэтому облако работает как раньше; если ключ задан секретом, поле не показывается, а написано, откуда он берётся. Ключи лежат в users-data/secrets.json – вне папки Click, поэтому переживают обновление и в репозиторий попасть не могут. | нет |
| 2026-08-06-gis-branches | – | **«Кабинет увёл не туда» у двадцати городов из двадцати одного – а он никуда не уводил.** У организации с филиалами кабинет переносит с `/company` на `/orgs/<id>/branches/<филиал>`, и это ТА ЖЕ страница «Данные о компании», только конкретного филиала – заголовок вкладки так и пишет: «Личный кабинет 2ГИС: Данные о компании — …». А проверка смотрела на адрес: нет в нём слова `company` – значит не туда. Самара филиалом не переехала, и там всё сработало с первого раза: надпись нашлась, клик мышью прошёл, кабинет ответил «Спасибо за подтверждение данных». Теперь «где мы» спрашивается у самой страницы, а не у адреса. **Удалённая карточка** больше не выглядит поломкой: если кабинет пишет «Компания удалена из справочника 2ГИС», город получает именно это и совет проверить статус в КП. **Жёлтая строка в логе** была отладочным дампом на сто шестьдесят знаков и пугала сильнее любой настоящей беды: теперь обычные города молчат, «не та страница» даёт одну короткую строку с заголовком и адресом, а разметка плашки печатается только в единственном непонятном случае – плашка есть, а надписи в ней нет. **Кнопка «Лог (.txt)»** у 2ГИС была вечно серой: лог сохранялся рядом с отчётом 2ГИС, а искался в папке Яндекса. **И скриншоты сбоя** больше не лежат мёртвым грузом – они показываются прямо в отчёте, по одному городу за раз. | нет |
| 2026-08-06-mem-guard | – | **Приложение падало со страницей «Oh no» на обычной публикации – две разные причины, обе устранены.** Первая: сторож памяти смотрел не туда. Замер брался из `/proc/self/status` – это память самого Python. А браузер Playwright живёт ОТДЕЛЬНЫМ процессом, и его 400–500 МБ в замер не попадали вовсе. Приложение видело «занято 300 МБ», спокойно пускало ещё один прогон, облако считало полтора гигабайта и убивало всё целиком – без единой строчки в логе. Теперь память спрашивается у самого контейнера (он считает всех своих детей), а если контейнер молчит – складывается своё дерево процессов. Считается живая память, а не кэш файлов: по кэшу сторож бил бы тревогу на ровном месте. И пороги теперь подстраиваются под потолок контейнера, но только в строгую сторону. **Заодно оказалось, что у ПУБЛИКАЦИИ сторожа не было вовсе** – он стоял только у актуализации. Поэтому публикация и падала молча. Теперь сторож один и тот же на обеих: перевалило за мягкий порог – перезапускаем браузер, за жёсткий – аккуратно встаём с сохранённым отчётом. Вторая причина: обновление кода на ходу. Облако обновляет файлы под работающим приложением, Streamlit в этот момент выселяет изменённые модули из памяти – и если в ту же секунду другая вкладка или идущий прогон импортирует такой модуль, импорт падает на ровном месте (`KeyError: 'paths'`, следом `'runner'`, `'llm'`). Приложение умирало целиком. Теперь свои модули прогреваются одним списком с повтором – выселение занимает мгновение, и повтор проходит. | нет |
| 2026-08-06-gis-confirm | `26f8bf6` (откат на `5d4f0d0`) | **«Данные верны» не находилась ни в одном городе – и это была не одна ошибка, а три, все из одной семьи: приложение ГАДАЛО вместо того, чтобы проверять.** Первая: готовность страницы определялась по словам «Данные о компании» в тексте – но так называется пункт меню слева, он есть на любой странице кабинета. Проверка была истинной ВСЕГДА, прогон объявлял страницу дорисованной через шесть секунд и уходил дальше, а плашку кабинет забирает отдельным запросом и рисует позже. Отсюда 6.5–9.3 секунды на город в логе и «плашки нет» подряд. Теперь готовность – это не найденное слово, а остановившаяся страница: ждём, пока текст перестанет меняться. Вторая: надпись искалась как элемент, ВЕСЬ текст которого равен «Данные верны». В живом кабинете это обычный div без роли и без фокуса, а надпись – кусок строки («…они не изменились? Данные верны») или «Да, данные верны». Такого элемента просто не существует. Теперь ищется место на экране, где нарисована фраза, и клик идёт мышью ровно туда – как пальцем. Третья: текст читался через `document.body.innerText`, который не заглядывает ни в теневые корни веб-компонентов, ни во врезанные кадры, – а кабинет собран именно так. Теперь читаются все корни сразу. И отдельно: успех больше не предполагается. Раньше «клик прошёл» засчитывалось как успех без единого доказательства; теперь нужен ответ кабинета – всплывшее «Спасибо за подтверждение данных» или исчезнувший вопрос. Нет доказательства – город получает честное «нажали, кабинет не ответил, подтвердите вручную» и скриншот экрана в отчёт. **Заодно:** города, где все отзывы отвечены, больше не получают пугающее «кабинет 2ГИС не отдал список». Пустой список опознавался по надписи «нет отзывов», а её формулировку кабинет волен сменить – ждали двадцать секунд знакомых слов и объявляли поломку. Теперь пусто – это устоявшаяся страница без карточек. **И на будущее:** если кабинет сменит слова на надписи, Click поищет короткий ответ ВНУТРИ плашки («Всё верно», «Данные актуальны») – но только внутри неё, по всей странице такие слова ловить нельзя, они встречаются в отзывах. А если и это не поможет, в лог уйдёт сама разметка плашки – следующая правка делается по ней сразу, без ещё одного круга со скриншотами. | нет |
| 2026-08-06-ekb-time | – | **Время везде – по Екатеринбургу.** В логе прогона стояло 11:56, когда в Екатеринбурге было почти 17:00: приложение показывало часы сервера, а сервер в облаке живёт по UTC. Дата при этом совпадала, и выглядело так, будто открыт какой-то посторонний прогон. Теперь по Екатеринбургу считается всё, что видит человек: строки живого лога, время в отчётах и в шапке прогона, имена файлов отчётов и дневных логов, отметка «последняя загрузка городов». Внутри и в файлах время по-прежнему хранится в UTC – так его можно сравнивать и не терять при переезде, – а переводится только на показ. Пояс задан числом (+5), а не именем `Asia/Yekaterinburg`: имена берутся из базы tzdata, которой на Windows может не оказаться, а Екатеринбург с 2011 года часы не переводит. | нет |
| 2026-08-06-fetch-missing | – | **Список организаций Яндекса отдаёт не всё – теперь недостающее добирается поимённо.** У заказчика в разделе «Организации» два Красноярска, список приносит один: второй заведён онлайн-организацией и в выдачу не попадает. Перечитывание не помогало – карточки там нет в принципе, и дубль не находился, сколько его ни ищи. Теперь при сборе Click берёт из КП ссылки на карточки и открывает напрямую те, которых список не дал. В отчёте появились метка сборки и предупреждение «Яндекс насчитал N организаций, а список отдал M» – раньше понять, свежим ли кодом сделан отчёт и всё ли собрано, было нечем. | нет |
| 2026-08-06-dupes-by-city | – | **Дубль = один город дважды в «Организациях», и всё.** Улица не важна, есть город в КП или нет – не важно, на какую строку КП карточка села – тоже не важно. Раньше лист «Дубли» строился от сопоставления: две карточки попадали туда, только если обе сели на одну строку КП. Теперь карточки объединяются по двум признакам сразу – общая строка КП и общий город из адреса, – поэтому дубль виден и когда город в КП не заведён, и когда карточки разошлись по разным строкам. Сетевые карточки в счёт не идут. Проверено на данных заказчика: по 50 её адресам ложных дублей ноль, а подложенная вторая карточка Красноярска находится. | нет |
| 2026-08-06-gis-banner | – | **«Плашки «Данные верны» нет» – хотя она есть.** Прогон писал так про Воронеж и Пермь, а на экране плашка стояла. Причина – срок ожидания: страница «Данные о компании» в облаке дорисовывается дольше, чем отпущенные ей восемь секунд (там же грузится карта). Теперь ждём до 25 секунд, но уходим раньше, как только страница готова и плашки на ней действительно нет. Главное же – прогон больше не выдаёт догадку за факт: если плашка на странице ЕСТЬ, а кнопку опознать не вышло, город получает честное «плашка есть, кнопку не нашли – подтвердите вручную», а если страница вообще не дорисовалась, так и написано. Раньше оба случая молча превращались в «подтверждать нечего» – ровно та неправда в отчёте, которую до этого чинили у Яндекса. Сама надпись теперь ищется по всем элементам, а не только по кнопкам и ссылкам, и находится, даже если спрятана в двух вложенных span. **И второе:** пропущенный отзыв возвращается следующим прогоном. «Убрать из списка» убирает его с глаз, а в кабинете он так и остался без ответа – теперь придёт снова, уже со свежим черновиком. Отвеченные так не возвращаются: они и не соберутся. | нет |
| 2026-08-06-dupes-any-city | – | **Дубли ловятся у любого города, а не только у тех, что есть в КП.** Два Красноярска в «Организациях» – это дубль, даже если город в таблицу не внесён или вторая карточка по адресу не опозналась; такие строки идут на лист «Дубли» с пометкой «(города нет в КП)». Попутно починен разбор адреса, из-за которого «городом» становилась улица: Яндекс пишет «Тамбовская улица» (слово в конце), а «корп. 2» не отсекалось из-за точки в сокращении – на живом адресе Красноярска городом выходило «стр. 1». Город теперь берётся по повтору: Яндекс называет его дважды («городской округ Красноярск, Красноярск»). Добавлены казахские приставки («городской акимат Актау»), промзона и номера домов вида «34/В». На живых данных заказчика 49 адресов из 50 разбираются верно; пятидесятый – «село Абай» при городе КП «Уральск», и это расхождение самих данных. | нет |
| 2026-08-06-stale-list | – | Отчёт сверки путал две разные беды. Если ссылка из КП ведёт на карточку, которой нет среди прочитанных организаций, писалось «ссылка в КП ведёт на другую карточку» – а карточка на месте, просто список организаций собран раньше, чем её завели. Теперь так и сказано: «карточки из ссылки в КП нет среди прочитанных организаций – перечитайте список». Прежняя формулировка осталась для своего случая: карточка в аккаунте есть, но привязана к другому городу. На листе «Как читать» появился раздел «Если в отчёте меньше карточек, чем видно в Яндексе». Сама проверка дублей исправна – проверено на данных заказчика: две карточки Красноярска дают статус «несколько» и две строки на листе «Дубли». | нет |
| 2026-08-06-gis-fulltext | – | **Текст отзыва 2ГИС доезжал только наполовину.** Сначала в очередь попадал хвост («адыков Адиль! То не берет телефон…»), после первой починки – начало с обрывом («…Прошёл 17дней, каждый день обнадеживали что есть ! Менеджер С…»). Два симптома одной причины: длинный отзыв кабинет сворачивает и держит ТРЕМЯ узлами разметки – видимая часть, отдельное многоточие и скрытый остаток. Читалка сначала брала «самый длинный кусок» (это оказывался скрытый остаток), потом – то, что видно на экране (это оказывалось начало с многоточием). Теперь текст собирается по кускам из самой разметки: узлы склеиваются ВПЛОТНУЮ (свёртка режет строку посреди слова, «Менеджер С» + «адыков Адиль» снова становятся «Менеджер Садыков Адиль»), а многоточие свёртки выбрасывается. Заодно номер отзыва считается по началу текста, а не по всему: иначе один и тот же отзыв, прочитанный то целиком, то обрезанным, ложился бы в очередь двойником. | нет |
| 2026-08-06-gis-reviews | – | **2ГИС собирал уже отвеченные отзывы и обрезал текст.** Два промаха в читалке, оба вылезли на первом боевом прогоне. Первый: список открывался адресом `…?withoutAnswer=true`, и мы верили, что там только неотвеченные. Оказалось, кабинет сначала рисует ВСЕ отзывы и лишь потом применяет фильтр – читалка успевала схватить предварительный список, и в очередь попадал отзыв, на который ответили ещё в ноябре. Теперь адресу не верим: ответ опознаётся по самой карточке (у ответа своя дата – дат становится две – и рядом появляются «Удалить» и «Отображать как основной»), а список ждём, пока он устоится: два чтения подряд с одинаковым числом карточек. Кнопка «Ответить» на отвеченной карточке остаётся – 2ГИС разрешает ответить ещё раз, – так что по ней отличить было нельзя. Второй промах: текстом отзыва брался «самый длинный листок разметки», а свёрнутый текст (line=3) 2ГИС держит кусками – в очередь попадал обрывок с середины слова: «адыков Адиль! То не берет телефон…». Теперь берётся блок целиком, куски склеиваются. Заодно в списке ответов появилась кнопка «Очистить список»: разбирать по одному то, что нападало от неудачного сбора, слишком долго. | нет |
| 2026-08-06-gis-login | – | **Вход в 2ГИС падал с «Locator.click: Timeout 30000ms exceeded» – и молчал о причине.** Поля искались селектором `input[type=text]`, а у 2ГИС форма собрана самодельными компонентами: атрибута type у поля почты может не быть вовсе (в разметке кабинета встречается голое `<input class="…" placeholder="Поиск">`). Ничего не находилось, Playwright полминуты ждал несуществующее поле, и человек получал техническую строку вместо объяснения. Теперь поля ищутся разбором страницы: сначала по подписи («почта», «логин», «телефон»), потом первое подходящее, поле поиска в шапке за почту не принимается. И главное: неудача больше не молчит – Click показывает СНИМОК страницы кабинета и говорит словами, что увидел («полей на странице: 2 – «Поиск», «Пароль»») или что ответил сам 2ГИС («Неверный логин или пароль»). Вход теперь чинится по снимку, а не по догадкам. Заодно убран значок карты у заголовков 2ГИС – на Windows он выглядел серым пятном. | нет |
| 2026-08-06-mpi-priyatno | – | Модель обрезала оборот до «Приятно, что …» – заказчик просила полный «Приятно слышать, что …». Промпт это оговаривает прямо, код достраивает: «Приятно, что», «Нам приятно», «Очень приятно» в начале абзаца становятся «Приятно слышать». Порядок замен поправлен – «очень» снимается раньше, иначе «Очень приятно» не считалось началом абзаца. | нет |
| 2026-08-06-city-pick | – | **«Выбираю все в КЗ, хочу ещё выбрать РБ – сбрасывается КЗ».** Выбрать все города в одной стране, раскрыть вторую и отметить там город – и первая страна слетала: из семи оставалась одна галочка. Причина в устройстве Streamlit. Кнопка «Снять все» писала значение ВСЕМ галочкам, включая города свёрнутых стран, у которых виджета на странице нет – для Streamlit это обычная запись в session_state, и она там оставалась. Потом страну раскрывали, значения становились виджетными, а при сворачивании виджет исчезал – и наружу вылезала та самая старая запись False. Подбор значений (он лечит другую беду – снятую галочку при быстром сворачивании страны) честно читал её как «человек снял галочку». Теперь Click помнит, какие галочки реально нарисованы, и трогает только их: и подбор, и «Выбрать все» работают по видимому экрану. Заодно галочка стала отражением набора выбранных, а не спорящей с ним стороной. Проверяется настоящими кликами мыши – tests_ui_click.py. | нет |
| 2026-08-06-mpi-wording | – | Ответы МПИ: «Нам очень приятно» звучало натужно – стало «Приятно слышать». Слово «очень» убрано вовсе: «Очень рады» → «Мы рады», «очень широкий выбор» → «широкий выбор». Промпт это запрещает, код подчищает за моделью. «Приятно слышать» в абзаце теперь ровно одно: второе такое же в середине заменяется на «Мы рады» – два подряд читались как заевшая пластинка. | нет |
| 2026-08-06-mpi-no-double | – | У МПИ ответ благодарил за отзыв дважды: «Спасибо за отзыв и рекомендацию!», а следом «Благодарим вас за отзыв!». Абзац теперь начинается сразу с сути – «Приятно слышать, что …». Промпт это требует и прямо запрещает второе спасибо; код на всякий случай убирает его сам, если модель всё же напишет. Благодарность с подробностями («Благодарим за то, что отметили скорость доставки») остаётся: это не дубль, а по существу. | нет |
| 2026-08-06-queue-trap | – | **«Загрузила всю очередь на публикацию, а кнопки нет».** Добавили последнюю страну – выбор стран сбрасывается, следующей уже не осталось, и ранний выход из функции уносил со страницы весь блок «В очереди к сохранению» вместе с кнопкой «Сохранить очередь в задачи». Собранное сохранить было нечем: плитки стран стояли помеченные «в очереди», а «Запуск» показывал пустую очередь – потому что в задачи ничего и не записалось. Плашка при этом бодро советовала «сохраните очередь ниже», а ниже ничего не было. Теперь блок очереди рисуется всегда, а вместо «выберите страну» показывается «все страны уже добавлены, осталось сохранить». | нет |
| 2026-08-06-2gis | – | **Вторая площадка: 2ГИС.** Click умеет то же, что в Яндексе, но в кабинете 2ГИС: заходит в «Данные о компании» и жмёт «Данные верны», если 2ГИС просит подтвердить данные; открывает отзывы сразу с фильтром «Без ответа» (это адрес `…/reviews?withoutAnswer=true`, кнопку жать не надо), пишет черновики тем же промптом и по тем же правилам (пять звёзд – черновик, ниже – человеку), а подтверждённый ответ публикует: «Ответить» → текст → «Опубликовать». Успехом считается только подтверждённый ответ – плашка «Ответ на отзыв размещён» или ответ, вставший под отзывом. Устройство кабинета другое: это React-приложение, готового состояния страницы (как `window.__PRELOAD_DATA` у Яндекса) там нет вовсе, поэтому разбираются карточки, а карточка ищется от звёзд вверх по составу – классы у 2ГИС случайные и меняются с каждой сборкой. Оценка считается по ширине жёлтого ряда звёзд. Отзывы с Flamp, Otello, Booking, НетМонета, Т-Банка и СберЧаевых, которые 2ГИС показывает вместе со своими, в Gemini не уходят: ответить на них оттуда нельзя, они попадают в список со ссылкой «ответить можно только там». Города берутся из блока «2ГИС» той же КП; статусы у площадок независимы – карточку могли удалить в Яндексе и оставить живой в 2ГИС. Прогон 2ГИС не пускается рядом с яндексовым: два тяжёлых браузера облако не тянет. Проверяется всё на настоящей странице кабинета – `tests_fixtures/gis-reviews-page.html`. | нет |
| 2026-08-06-kp-selfsync | – | **Города из КП больше не откатываются к списку из кода и обновляются сами.** Заказчик загрузила города МПЭ из КП, ночью облако перезапустилось – утром в списке были вчерашние. Причина: без локального конфига Click собирал его из пресета `projects_data.MPE_CITIES`, а внешнюю копию подставлял только «если локально пусто». Пресет не пустой – свежий список молча проигрывал коду; у МПИ пресета нет, поэтому там всё работало и беда выглядела случайной. Теперь пресет – только для проекта, который ещё ничего не сохранял. Плюс список подтягивается из таблицы сам, если с последней загрузки прошло больше шести часов: КП правят руками, и прогон по вчерашнему списку означал клики по карточке, вчера отмеченной «Удалена». Кнопка «Загрузить» осталась – для «прямо сейчас», и ходит той же дорогой. Таблица недоступна – работаем на прежнем списке и говорим об этом, но не долбимся. | нет |
| 2026-08-06-session-mpi | – | **Прогон вставал на слетевшей сессии – теперь он останавливается.** Актуализация не проверяла, не увёл ли Яндекс на страницу входа. Кнопки «Данные актуальны» там, разумеется, нет – и каждый из 58 городов получал «актуализация не требуется». Это неправда, и она уходила в отчёт как успех. Теперь страница входа распознаётся, прогон встаёт на первом же городе и говорит: зайдите в «Настройки» и войдите в Яндекс заново. Публикация так умела давно. То же самое на шаге отзывов. **Ответы МПИ приведены к виду заказчика.** Три образца из письма легли в промпт дословно, первая строка «Спасибо за отзыв и рекомендацию!» больше не варьируется, абзац благодарности всегда заканчивается фразой про ассортимент. Общее правило «ассортиментная фраза – отдельный абзац» к МПИ больше не приклеивается: там она внутри абзаца, и правило уводило модель от нужного вида. Форму дополнительно чинит код: первая строка, фраза про ассортимент и подпись ставятся на место, даже если модель их потеряла. | нет |
| 2026-08-06-memory-guard | – | **Сторож памяти: приложение больше не должно умирать молча.** Заказчик запустила актуализацию с отзывами в одном браузере, во втором зашла в другой проект и запустила там актуализацию и сверку – всё легло. В логе ни одной питоновской строки: процесс убило облако по памяти. Две причины. Первая – дыра в защите, поставленной накануне: общий потолок в два прогона пропускался, если в ЭТОМ проекте уже что-то шло, и третий прогон пролезал именно так. Поблажка убрана, потолок теперь без исключений и называет, что именно занято. Вторая, более важная: память вообще никто не мерил, прогон шёл вслепую до самой смерти. Теперь измеряется фактическая память процесса и есть три порога: 1200 МБ – новый прогон не начинаем; 1500 МБ – посреди прогона перезапускаем браузер (возвращает сотни мегабайт); 1900 МБ – прогон аккуратно останавливается с сохранённым отчётом. Вместо «облако убило всех» получается «один прогон закончился раньше времени, и об этом написано словами». Занятая память пишется в ИТОГИ каждого прогона. | нет |
| 2026-08-06-no-none | – | **Страница забивалась словом «None» и подвисала.** Снял галочку города – и на экране сто одна строка «None», по одной на город. Виновата «магия» Streamlit: он переписывает главный скрипт и ЛЮБОЕ голое выражение заворачивает в вывод на экран. Вызовы функций пропускает, а условное выражение «A if cond else B» – нет; у нас стояло `sel.add(cid) if ... else sel.discard(cid)`, и оба метода возвращают None. Заходит эта магия и внутрь функций, не только в тело модуля. Переписано обычным if/else. Появился тест, который разбирает главный скрипт и запрещает голые выражения, кроме вызовов и строк-документации, – он тут же нашёл ещё два таких же места в выборе городов, написанных так же и по той же привычке. | нет |
| 2026-08-06-nolink-sheet | – | Новый лист отчёта сверки «Нет ссылки в КП». Пометка «нет ссылки в КП – возьмите из колонки «Ссылка на карточку»» стояла в строках «Сверки», и цифра на дашборде была, а собрать все такие города в одном месте было негде – приходилось идти по строкам глазами. Теперь это отдельный лист: страна, город, название и адрес из Яндекса и ссылка на карточку. Города с несколькими карточками помечены отдельно – там сначала на лист «Дубли». На данных заказчика лист собирает 87 городов из 181 найденных. | нет |
| 2026-08-06-no-flash | – | **«При нажатии «Выбрать все в стране» всё растягивается на секунду».** Наш блок стилей лежал среди обычных элементов страницы, а Streamlit при перерисовке их пересобирает: замер на живой странице показал 450 мс без оформления – всё это время экран показывал голую вёрстку Streamlit (вкладки становились переключателями, кнопки – во всю ширину). Стиль теперь дублируется в <head>, куда Streamlit не заглядывает, и переживает любую перерисовку. Заодно: поле со ссылками тянется вслед за загрузчиком – раньше правый блок рос под список файлов, а слева оставалась дыра в ту же высоту. | нет |
| 2026-08-06-upload-fit | – | Прикреплённые фото вылезали за рамку карточки: у зоны загрузки была жёсткая высота – ровно по полю со ссылками слева, – и список файлов в неё не влезал. На четырёх файлах последняя плитка уходила на 14 пикселей ниже рамки, а кнопка «+» – на 54. Теперь эта высота стала минимальной: пустая зона по-прежнему вровень с полем, а с файлами растягивается под их список. Длинное имя файла больше не распирает плитку вбок – ужимается многоточием, крестик остаётся на месте. Исправлено в обоих местах, где прикрепляют фото: картинки поста и фото в раздел «Товары». | нет |
| 2026-08-06-no-reload | – | **Приложение падало с KeyError: 'runner' прямо на импорте.** Заказчик запустила актуализацию вместе со сверкой КП – «Oh no». Виновата была наша же самодельная перезагрузка модулей: она шла из пользовательского потока и правила sys.modules, пока соседний поток эти же модули импортировал. Потоков стало много – два прогона разом, плюс своя вкладка у каждого человека. Перезагрузку убрали совсем: ровно ту же работу Streamlit делает сам и делает безопасно (его local_sources_watcher выселяет изменённые модули так, чтобы «never mutate sys.modules while user code is running»). Метка сборки осталась – её показываем человеку и используем ключом кэша CSS. Заодно потолок в два прогона стал общим на всё приложение, а не на проект: двое за разными компами на разных проектах подняли бы по два браузера каждый, а память у облака одна на всех и браузер весит около 450 МБ. | нет |
| 2026-08-06-checkbox-race | – | Снятая галочка города терялась, если тут же свернуть страну – «снять можно только кнопкой». Гонка: браузер отправляет снятие, ответа ещё нет, а человек уже жмёт по стране; второй запрос приходит с обоими изменениями, страна сворачивается – галочки в этот проход не рисуются, значит on_change по ним не зовётся и снятие пропадает. Локально почти не ловится, в облаке с его задержками – запросто; кнопка «Снять все» пишет напрямую, поэтому работала. Теперь значения галочек читаются напрямую ДО отрисовки: невидимые виджеты Streamlit вычищает только ПОСЛЕ прогона скрипта, так что присланное значение в этот момент ещё доступно. Галочка «Заодно проверить отзывы» включена по умолчанию. | нет |
| 2026-08-06-send-button | – | Кнопка «Отправить все» стала фиолетовой – это главное действие раздела, а серая терялась среди соседних. Текст подтверждения поправлен по формулировке заказчика: «Они появятся на карточках под именем бренда, удалить можно будет только вручную». | нет |
| 2026-08-06-run-scope | – | Плашка «Вам на ответ» переименована в «Негативные». Разобранные отзывы на странице теперь показываются только за ТЕКУЩИЙ прогон: заказчик собрала новые отзывы, а в «Отчёте по отправке» видела три отправленных с какого-то прошлого раза. Каждый отзыв помечается прогоном, в котором найден; на странице – свежий прогон, вся история – во вкладке «Отчёт», у каждого прогона свой список отзывов и своя выгрузка. Ждущие ответа показываются ВСЕ, даже со старых прогонов: черновик написан и терять его из виду нельзя. Отчёт прогона, в котором отзывы не сохранялись, собирается по метке прогона из очереди – старые прогоны тоже открываются. | нет |
| 2026-08-06-layout-limits | – | Вид «Актуализации» перебран по замечаниям заказчика: сверху сам прогон, ответы на отзывы ушли ВНИЗ (раньше экран открывался на них, и сколько городов пройдено видно не было), лог сворачивается всегда – во время прогона раскрыт, после свёрнут. Над списком ответов появились плашки-карточки тем же видом, что в отчёте: черновик готов, вам на ответ, без черновика, отправлено, не отправились. Светлая тема при заходе вместо тёмной – и в базовых цветах Streamlit тоже, чтобы не мигало. Упёрлись в лимит Gemini дважды подряд – черновики до конца прогона не пишем: в логе было по четыре одинаковых отказа на город и шесть минут впустую, а квота за минуту сама не вернётся. Отзывы при этом собираются ВСЕ, черновики дописываются кнопкой «Переписать все». «Остановить» теперь слышно между черновиками, а не только между городами – на карточке с десятком отзывов кнопка отзывалась минут через пять. | нет |
| 2026-08-06-report-reviews | – | Отчёт об актуализации теперь собирается вместе с отзывами. В отчёт прогона кладутся сами отзывы, а не только их количество, и появился раздел «Отзывы этого прогона» теми же строками, что и города. Скачать можно всё тремя кнопками: «Города (CSV)», «Отзывы (CSV)» и «Лог». Тонкость в том, что отправка идёт ПОСЛЕ прогона и вручную: отчёт знает, что нашлось, а что с ответом стало – знает очередь, поэтому выгрузка склеивает одно с другим, и в ней виден живой статус, время отправки и итоговый текст. Отдельно: ассортиментная фраза теперь гарантированно уходит дословно. Один опубликованный ответ ушёл со строкой «трубы, armatura – собираем и отгружаем без задержек»; сплошной запрет латиницы поставить нельзя (AISI 304, DIN, A2 законны), поэтому фраза каждого бренда записана эталоном и похожий на неё абзац заменяется на эталонный. | нет |
| 2026-08-06-parallel-runs | – | **Прогоны можно гонять параллельно.** Раньше лок был один на проект: пока шла актуализация, «Прочитать организации» отбивалось «уже идёт другой прогон», хотя чтение ничего в Яндексе не меняет. Теперь у каждого вида прогона свои лок, состояние, живой лог и кнопка «Остановить» – вкладка показывает свой прогон и не видит соседний. Чтение организаций уживается с чем угодно; две публикации или две актуализации разом по-прежнему запрещены (дубли постов и двойные клики по городу), публикация с актуализацией – тоже, но по другой причине: это два тяжёлых браузера, а бесплатному облаку хватало и одного. Потолок – два прогона разом. Отказ теперь объясняет, что именно мешает. Попутно: логгер браузера стал раздельным по потокам (иначе строки одного прогона писались бы в лог другого, а вкладки, которые водят браузер сами, писали бы в лог чужого прогона), и сессия Яндекса сохраняется через временный файл – два прогона больше не могут застать её на середине записи. | нет |
| 2026-08-06-pace-caption | – | Вкладка «Актуализация» падала с AttributeError. Из llm.py убрали константу MIN_GAP_S (пауза уступила место подсчёту запросов в минуту), а в подписи «переписать все – примерно N мин» ссылка на неё осталась. Оценка теперь считается по текущему темпу Gemini. Поиском промах не нашёлся: искали `_gap`, а имя написано прописными – поэтому появился тест, который разбирает КОД всех файлов и проверяет, что каждое обращение вида «модуль.имя» ведёт к существующему имени. Разбирается именно код, а не текст: «build.py» из комментария ложной тревоги больше не даёт, а локальная переменная с именем модуля не путается с самим модулем. | нет |
| 2026-08-06-one-browser | – | **Массовая отправка клала приложение: «еле как 6 штук сделал и сломался».** В логе облака при этом тишина и мёртвый сервер, без единой питоновской ошибки – процесс убивали по памяти. Причина в устройстве самого Streamlit: на каждую перерисовку он создаёт для главного скрипта НОВЫЙ модуль и выполняет файл заново, поэтому любая переменная модуля после каждого шага снова пустая. Ручка открытого браузера и текущий город лежали именно там, а пачка обрабатывает по одному ответу за перерисовку – ссылка терялась ровно между ответами. Отсюда три симптома при одной причине: старый Chromium никто не закрывал и на следующий ответ поднимался ещё один (шесть ответов – шесть браузеров); «карточка этого города уже открыта» не срабатывало никогда, и одна и та же карточка грузилась заново на каждый ответ; счётчик «перезапускать браузер каждые пять» не доживал до пяти. Всё, что должно пережить перерисовку, переехало в session_state. Перезапуск браузера каждые пять ответов убран как затычка: теперь один браузер на всю пачку и одна вкладка на город – пять ответов Еревану уходят на одну открытую страницу, на смене города вкладка пересоздаётся и память освобождается сама. Замерено на настоящем Chromium: по старой схеме память росла примерно на 440 МБ с каждым ответом (407 → 848 → 1341 → 1781 МБ за четыре ответа) – к шестому облако и убивало приложение; по новой 32 ответа по восьми городам идут на ровных ~458 МБ, число процессов не растёт. | нет |
| 2026-08-06-keys-pace | – | **Ключей несколько, а лог жёлтый от «придерживает по лимиту».** Три причины, все настоящие. Первая: ключи не чередовались. Брался первый ключ, которым можно слать прямо сейчас, а первым в списке всегда идёт первый – пока пауза мала, он успевал освободиться к следующему отзыву, и до второго очередь не доходила. Заказчик заметила это сама: «один ключик до конца выжимаем и идём выжимать второй». Теперь ключи идут строго по кругу, каждому достаётся равный отдых. Вторая: лимит Google считается НА ПРОЕКТ, а не на ключ – ключи из одного проекта делят одну квоту и скорости не добавляют вовсе. Click теперь распознаёт это по факту (отказ получил ключ, которым за минуту почти не пользовались – значит квоту выбрал сосед) и пишет об этом словами в логе и в «Настройках»: новый ключ нужно создавать в НОВОМ проекте. Третья: пауза подбиралась вслепую – начинали с 1.5 секунды, на каждый отказ удваивали. Разгон успевал выбрать минутную квоту, дальше сыпались отказы, пауза улетала в потолок, и на черновик уходило 40 секунд и четыре запроса, из которых три отвергнуты. Теперь считаем как Google – запросы за последние 60 секунд, отдельно по каждой модели, – и ждём ЗАРАНЕЕ, а планку темпа узнаём по первому отказу и дальше держим сами. Пауза между кругами убрана, потолок времени на черновик поднят до 100 секунд: ожидание стало осмысленным, дождавшись, получаем черновик наверняка. | нет |
| 2026-08-06-audit-report | – | Отчёт сверки переделан: «ничего не понятно» – одна таблица в сорок колонок отвечала сразу на все вопросы. Теперь книга из восьми листов, у каждого своя задача: «Дашборд» цифрами, «Как читать», «Нет в КП» со ссылкой на каждую карточку, «Дубли», «Нет в Яндексе», «Расхождения», сетка ✓/✗ по городам и полное КП. Оформление как в отчёте site-checker «Проверка КП»: значки вместо простыней текста, подробности примечанием к ячейке, заливка только у проблемных. Исправлено ложное «ссылка в КП ведёт на другую карточку» почти на каждой строке: в КП ссылки записаны старым номером карточки (tycoon_id), а список организаций отдаёт новый – это одна и та же карточка. | нет |
| 2026-08-06-server-pins | – | **«Oh no. Error running app», ребут не помогал.** Ломался не наш код – до него дело не доходило. Streamlit тянет за собой библиотеку starlette и разрешает любую версию младше 2.0, а облако при пересборке ставит самую свежую. В starlette 1.4.0 у сжатия ответов появился новый обязательный аргумент, которого Streamlit 1.60.0 не передаёт: сервер падал на каждом запросе и отвечал 500 даже на проверку живости. Версия starlette закреплена на последней рабочей – 1.3.1. Ребут не помогал именно потому, что окружение пересобиралось тем же составом; правка requirements.txt заставляет собрать его заново. Заодно закреплён весь «мотор» – десять библиотек, из которых собран сам веб-сервер (uvicorn, h11, httptools, anyio, websockets, python-multipart, itsdangerous, watchdog, protobuf): их поломка означает белый экран без диагноза, тогда как всё остальное ломается уже внутри открывшегося приложения и чинится быстро. Версии взяты те, что облако само успешно поставило на Python 3.14 и на которых прогнаны тесты; тест сверяет, что в файле записано ровно то, на чём мы тестируем. | нет |
| 2026-08-06-one-build | – | **Приложение не поднималось, и перезапуск не помогал.** Метка сборки была записана в каждом модуле своей строкой, и при параллельной работе у kp_audit осталось старое значение. Проверка «все ли модули из одной сборки» стала ложной навсегда – Click перезагружал все десять модулей на каждое нажатие, съедал память облака и падал; после перезапуска первое же действие всё повторяло. Метка переехала в build.py, одна на всё приложение: забыть обновить её в одном из файлов больше негде. Тест следит, чтобы своей строки BUILD в модулях не появилось снова. | нет |
| 2026-08-06-send-report | – | Отправка четырнадцати ответов клала приложение по памяти. Причина: на каждый ответ шло до четырёх полных перезагрузок тяжёлой страницы отзывов, и в Python тянулся весь блок состояния страницы. Теперь со страницы забираются только нужные поля и прямо в браузере; ответы одного города идут по одной открытой странице; результат проверяется по живой странице, а не перезагрузкой; браузер перезапускается каждые пять ответов. Появился «Отчёт по отправке»: город, автор, время, исход, причина ошибки, выгрузка CSV. Исход каждого ответа сохраняется наружу сразу – после падения видно, что успело уйти. | нет |
| 2026-08-06-kp-audit | – | Новый раздел «Сверка» после «Настроек». Click читает все организации аккаунта, раскладывает их по городам КП и сравнивает сайт, телефоны и почту. Города с несколькими карточками помечаются отдельно – это дубли. На выходе то же КП плюс колонки «Я: …» и лист «Лишние в Яндексе». Организации и карточки читаются из данных самой страницы Яндекса, а не из вёрстки: список отдаёт адрес, сайт, телефоны и почту сразу, поэтому 200 карточек открывать не нужно. Лист КП выбирается вручную (нужен «Лист20»). | нет |
| 2026-08-06-gemini-keys | – | Ключей Gemini теперь может быть несколько (gemini_api_key_2, gemini_api_key_3). Лимит на бесплатном тарифе считается на каждый ключ отдельно, поэтому два ключа – вдвое быстрее, три – втрое. Запросы идут по парам «ключ + модель»: отказал по лимиту – ключ на минуту откладывается, работа продолжается остальными. Сообщение об упоре в лимит стало человеческим и подсказывает добавить второй ключ. | нет |
| 2026-08-06-batch-stop | – | У «Переписать все» и «Отправить все» появилась кнопка «Остановить». Раньше пачка крутилась циклом внутри одной перерисовки: страница замирала на «2 из 16», рядом висело «Упёрлись в лимит Gemini», и прервать это было нечем. Теперь за перерисовку обрабатывается одна штука, между ними страница живая. После остановки в итоге написано, сколько сделано и сколько осталось; уже сделанное сохранено. Браузер для отправки держится открытым между шагами и закрывается в конце. | нет |
| 2026-08-06-reviews-rules | – | Левая часть шапки стояла ниже плашки проекта и кнопки темы – обнулены отступы обёрток Streamlit, блок приподнят. Правила ответа собраны в один общий список для всех брендов: назвать в ответе то, что клиент купил (труба, арматура, объём) – а если конкретики в отзыве нет, не выдумывать; ассортиментная фраза отдельным абзацем и без добавок; «вы» со строчной. Имя латиницей («Michail», «Felix») больше не подставляется – пол по нему не прочитать, пишем «Уважаемый клиент». | нет |
| 2026-08-06-send-all | – | Кнопка «Отправить все»: подтверждённые ответы уходят пачкой за один запуск браузера, с подтверждением и полосой прогресса. В пачку берутся только готовые целые черновики – отзывы «отвечаете сами» и неудачные остаются на ручной разбор. Результат по каждому пишется сразу, обрыв на середине ничего не теряет и не отправляет повторно. Слово «негодный» в интерфейсе заменено на «неудачный». | нет |
| 2026-08-06-kp-sheet-reviews | – | Публикация ответа проверена на живой карточке – работает. Один черновик занимал минуты: на отказ по лимиту шло четыре повтора с паузами до минуты, и так по каждой из трёх моделей. Теперь один запрос на модель по кругу, отказ – сразу следующая модель со своей квотой, общее время на черновик ограничено 75 секундами. В лог пишется, сколько заняла генерация и сколько было запросов. Абзац благодарности у АПС – ровно три коротких предложения и 150-230 знаков, чтобы не был вдвое длиннее ассортиментной фразы. | нет |
| 2026-08-05-kp-sheet-pick | – | Click брал старый лист «карта присутсвия … (OLD» с четырьмя городами вместо рабочего «кп», где их 101. Теперь учитывается gid из ссылки (человек прямо указывает лист), а старые копии и «НЕ ТРОГАТЬ ОРИГ» уходят в конец очереди. | нет |
| 2026-08-06-kp-xlsx-reviews | – | Кнопка отправки ответа ищется по своему классу BusinessResponseDraft-SendButton – прошлый поиск «кнопка, которой раньше не было» промахивался, потому что кнопка есть в разметке всегда, до ввода она свёрнута. Длинные тире запрещены по всем брендам: убраны из промптов и заменяются в готовом ответе. Обращение выбирается по полу («Уважаемый Иван», «Уважаемая Анна»), при неясности – «Уважаемый клиент»; «Уважаемый(ая)» со скобками считается негодным черновиком. Пауза между запросами к Gemini стала адаптивной: 1.5 сек, растёт только после отказа – генерация заметно быстрее. | нет |
| 2026-08-05-kp-xlsx | – | КП, залитая на Диск как файл Excel, теперь читается – качаем файл через Drive API, как давно делает site-checker. Конвертировать таблицу не нужно. Выбор городов для актуализации сверяется с текущим списком: после загрузки из КП новые города больше не остаются невыбранными молча. | нет |
| 2026-08-05-reviews-style | – | Промпт АПС переписан по эталонам заказчика: ответ называет конкретику из отзыва своими словами («заказом 12 тонн арматуры», а не «оценили качество»), замечание клиента отрабатывается одной фразой без оправданий, ассортиментная фраза идёт отдельным абзацем дословно, абзацы выровнены по длине. В промпт легли три эталонные пары «отзыв → ответ». Кнопка «Переписать все» теперь берёт все черновики, а не только негодные, – после правки промпта устаревают все сразу. | нет |
| 2026-08-05-reviews-view | – | «Переписать» работала, но экран этого не показывал: Streamlit держался за первый текст, который человек увидел, и поле оставалось со старым черновиком, хотя в файле лежал новый. Теперь ключ поля зависит от самого черновика – переписали, значит и на экране новый. Плюс негодный черновик распознаётся не только по обрыву: ловим следы «мыслей вслух» модели («Line 3:», «MUST be 2 sentences») и ответы, съехавшие на английский. | нет |
| 2026-08-05-reviews-send | – | Отправка ответа не работала: кнопка у Яндекса – кружок со стрелкой без единой буквы, а Click искал её по тексту «Отправить». Не находил, жал Ctrl+Enter, Яндекс его игнорировал – и Click всё равно рапортовал об успехе. Теперь кнопка ищется как та, что появилась ПОСЛЕ ввода текста, а «отправлено» пишется только когда Яндекс сам показывает ответ на перечитанной странице; иначе отзыв остаётся в списке. Добавлена кнопка «Переписать все» для черновиков, оборванных прошлой версией. | нет |
| 2026-08-05-reviews-fix | – | Разбор первого боевого прогона отзывов. Вкладка падала с AttributeError: облако обновило главный скрипт, оставив reviews в памяти прежним – теперь метка сборки одна на все модули, при расхождении модуль перезагружается сам. Обращение по имени без фамилии («Уважаемый Павел!», а не «Уважаемый Павел Филиппов!»). В «Настройках» появилась кнопка «Проверить генерацию на примере». Готовые черновики не показывались: очередь запоминалась в памяти вкладки до прогона и больше не перечитывалась – теперь читается с диска при каждой перерисовке, а на вкладке «Актуализация» виден счётчик ждущих ответов. Все черновики обрывались на полуслове: свежая Gemini тратила лимит на размышления – размышления отключены, лимит поднят, обрезанный ответ теперь не показывается вовсе. Лимит запросов Gemini: пауза между вызовами, ожидание по подсказке Google, переход на запасную модель. Потолок черновиков на город поднят с 5 до 20 – на живых карточках неотвеченных до 12, и восемь отзывов зря остались без ответа. | нет |
| 2026-08-05-reviews | – | Ответы на отзывы. В «Актуализации» появилась галочка «заодно проверить отзывы»: прогон находит отзывы без ответа компании и готовит черновики через Gemini по промпту бренда. В Яндекс ничего не уходит само – пары «отзыв → черновик» ждут подтверждения наверху вкладки. Черновик пишется только на пять звёзд, всё ниже уходит человеку со ссылкой. Перед отправкой отзыв перечитывается, чтобы не задублировать чужой ответ. Пять промптов заказчика перенесены дословно и правятся в «Настройках». README поправлен: проектов пять, СМУ – Стальметурал. | нет |
| 2026-08-05-critical-four | – | Закрыты последние четыре пункта критического списка: «Добавить пост» жмётся по элементу, лок различает живую чужую копию Click по пульсу, на город не больше трёх попыток, из нескольких ответов API берётся самый точный. | нет |
| 2026-08-05-goods-shipment | – | Поле «Фото в раздел Товары» показывается только у типа поста «Отгрузка», как в оригинале. | нет |
| 2026-08-05-no-duplicates | – | Перед публикацией Click смотрит ленту города: такой пост уже есть – не отправляет. Реестр публикаций переживает перезапуск облака. Уведомление «Данные актуализированы» наконец распознаётся. Недоступная карточка больше не выдаёт «кнопка не найдена». На «Актуализации» сначала запуск, потом панель прогона; вторая карточка отчёта убрана. | нет |
| 2026-08-05-report-tiles | – | Отчёт собран в один блок: дата справа в шапке, цветные плашки стали фильтрами (второй ряд кнопок убран), детали одним плоским списком с колонкой страны, скачивание внутри деталей. Кнопка «Обновить список» убрана. | нет |
| 2026-08-05-bulk-dupes | – | Добавление городов «Списком» ловит дубли (проверка была только у добавления по одному). Красная плашка над списком ошибок убрана. Страница отчёта не падает, если облако держит старый модуль в памяти. | нет |
| 2026-08-05-live-panel | – | Живая панель обновляется сама на ОБЕИХ вкладках (на «Актуализации» её не было – экран замирал на первом городе). Вкладка «Отчёт» показывает и актуализацию. После прогона лог сворачивается, появляется «Посмотреть отчёт». Кнопки «Отчёт (CSV)» и «Лог (.txt)», лог сохраняется отдельно для каждого прогона. Клик «Данные актуальны» теперь по элементу, а не по координатам. | нет |
| 2026-08-04-aps-cities | – | 62 города АПС зашиты в приложение (КП в таблице ещё не доделана). Click научился новому адресу карточек Яндекса `/business/companies/company/<id>/` – раньше такие города молча выпадали из прогона. | нет |
| 2026-08-04-project-aps | – | Пятый проект АПС (Авиапромсталь), города из его КП. Разбор КП научился читать статус с названием «Яндекс_Статус», отбрасывать «не активные» и сводить «РФ» и «Россия» в одну страну. | нет |
| 2026-08-04-queue-flow | – | Очередь как в оригинале: после «Добавить» страна помечается «✓ в очереди», следующая незаполненная выбирается сама, под кнопкой остаётся заметка. Отчёт актуализации – всегда 4 колонки. Пароль вписывается и жмётся одна кнопка «Далее», без Enter. Сессия Яндекса хранится в приватном хранилище и переживает перезапуски облака. | нет |
| 2026-08-04-no-self-sms | – | Click сам жал «Подтвердить» на экране «Безопасный вход» и этим запускал отправку SMS, хотя шёл за письмом. Теперь при входе по письму эта кнопка не нажимается никогда. Экран пароля перестроен: поле и его кнопка подряд, способы без пароля – отдельным блоком. | нет |
| 2026-08-04-ru-passport | – | МПИ переведён с @yandex.com на @yandex.ru: у трёх других проектов был .ru, и только у МПИ .com – он уводит в международный паспорт, где вход подтверждается звонком, а не письмом. В настройках появилось предупреждение с кнопкой «Исправить». | нет |
| 2026-08-04-mail-first | – | Подтверждение с почты стало основным способом входа – отдельной кнопки больше нет. Куки устройства запоминаются между входами, чтобы Яндекс не считал каждый раз новый компьютер. Кнопка «Другой способ входа», экран ожидания письма распознаётся отдельно. | нет |
| 2026-08-04-login-by-mail | – | Паспорт открывается по-русски (был английский – другой экран, другие кнопки). Вход по письму на почту отдельной кнопкой: пароль не вводится вовсе, поэтому Яндекс не навязывает звонок. Кнопки «Назад» и «Остановить вход» на каждом шаге. | нет |
| 2026-08-04-report-tiles | – | Отчёт актуализации: убрана плитка «Всего», время по местным часам, во время прогона видно, что отчёт заполняется на ходу. Полоса прогресса считает законченные города, а не начатые. Окно выбора файла больше не повисает поверх браузера. | нет |
| 2026-08-04-login-steps | – | Вход в Яндекс: экран определяется по надписям, показывается одно нужное поле. Разобраны телефон / логин / пароль / подтверждение / код / капча. Кнопки жмутся по элементу. Галочка «показывать окно браузера» переживает смену раздела. | нет |
| 2026-08-03-publish-hardening | `3d1294c` | Клик «Создать» по элементу вместо координат – публикация заработала. Проект МПИ, редактор окончаний, хранение городов и настроек в GitHub. | **да** – СМУ, Армения 3/3 |

## Важно про скачивание архива

Кнопка «Download ZIP» на GitHub отдаёт **ветку по умолчанию**, а не `main`.
Пока в настройках репозитория по умолчанию стоит рабочая ветка, она держится
вровень с `main` – архив приходит правильный. Если ветку по умолчанию
переключить на `main` (Settings → General → Default branch), связка станет
проще и надёжнее.
