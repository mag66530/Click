# Версии закреплены намеренно. С «>=» облако ставило любую свежую сборку
# Streamlit, а вёрстка опирается на его внутренний DOM (классы st-key-…,
# структура кнопки) – он между версиями меняется. Проверяем на этих версиях,
# на них же и работаем. Правка этого файла заодно заставляет Streamlit Cloud
# пересобрать окружение и перезапустить приложение начисто.
streamlit==1.60.0
# starlette тянется как зависимость Streamlit («starlette<2,>=0.40.0»), и облако
# ставило самую свежую. В starlette 1.4.0 у GZipResponder появился обязательный
# аргумент thread_minimum_size, а Streamlit 1.60.0 его не передаёт – сервер падал
# с TypeError на каждом запросе, приложение отвечало 500 ещё до запуска нашего
# кода («Oh no. Error running app», ребут не помогал). Держим последнюю рабочую.
starlette==1.3.1
playwright==1.62.0
google-auth>=2.20.0
requests>=2.28
openpyxl>=3.1.0        # КП, залитая на Диск как .xlsx – читаем файлом
