"""
build.py – метка сборки Click. Одна на всё приложение.

Зачем она. Streamlit Cloud умеет обновить главный скрипт, оставив соседние
модули в памяти прежними. Тогда свежая страница зовёт функцию, которой в
старом модуле ещё нет, – и вкладка падает. `streamlit_app.py` сверяет метку
у каждого модуля и при расхождении перезагружает их сам.

Почему метка переехала в отдельный файл. Раньше она была записана в каждом
модуле своей строкой, и это ровно один раз сломалось: две работы шли
параллельно, у `kp_audit` осталось старое значение – и проверка «все ли
модули из одной сборки» стала ЛОЖНОЙ НАВСЕГДА. Click перезагружал все
десять модулей на каждое нажатие, съедал память облака и падал, а
перезапуск не помогал: следующее же действие всё повторяло.

Теперь значение одно и лежит здесь. Забыть обновить его в одном из файлов
больше нельзя – его там просто нет. Менять при каждой правке, которая
меняет поведение модулей или вёрстку.
"""

BUILD = "2026-08-07-new-sprav-url"
